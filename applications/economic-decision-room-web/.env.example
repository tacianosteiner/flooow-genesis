# Local/investor runtime only. Never commit a real bearer token.
VITE_API_BASE_URL=http://127.0.0.1:8080
# Explicit opt-in for deterministic screenshots; never set in production.
VITE_DEMO_MODE=false
